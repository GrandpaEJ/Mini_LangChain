[package]
name = "mini_langchain"
version = "0.1.0"
edition = "2021"

[lib]
name = "mini_langchain"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.23", features = ["extension-module"] }
mini_langchain_core = { path = "../core" }
